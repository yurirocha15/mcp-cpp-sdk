# Release verification key contract

`release-signing-key.asc` is the publisher's canonical public verification
key. This template contains no public-key body, fingerprint, identity metadata,
private key, or provisioning evidence.
